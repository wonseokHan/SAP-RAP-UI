_schema-version: "3.2"
ID: travel
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travel-destination-content
  type: com.sap.application.content
  requires:
  - name: travel-destination-service
    parameters:
      content-target: true
  - name: travel-repo-host
    parameters:
      service-key:
        name: travel-repo-host-key
  - name: travel-uaa
    parameters:
      service-key:
        name: travel-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travel_html_repo_host
          ServiceInstanceName: travel-html5-srv
          ServiceKeyName: travel-repo-host-key
          sap.cloud.service: travel
        - Authentication: OAuth2UserTokenExchange
          Name: travel_uaa
          ServiceInstanceName: travel-xsuaa-srv
          ServiceKeyName: travel-uaa-key
          sap.cloud.service: travel
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travel-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travel-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travel.zip
      name: travel
      target-path: resources/
- name: travel
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travel-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travel-destination-service
    service-plan: lite
- name: travel-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travel-xsuaa-srv
    service-plan: application
- name: travel-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travel-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
